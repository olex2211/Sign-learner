# Gesture Assets

Source gesture media for Ukrainian Sign Language.

These files are committed for quick local startup. They are imported into runtime storage with:

```powershell
python server/scripts/import_gesture_media.py
```

Runtime copies are written to `server/media/gestures/...` and should stay ignored by Git.
